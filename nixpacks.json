{
  "phases": {
    "setup": {
      "nixPkgs": ["nodejs"],
      "aptPkgs": ["curl", "wget"]
    },
    "install": {
      "cmds": ["npm install"]
    },
    "build": {
      "cmds": ["npm run build"]
    }
  },
  "start": {
    "cmd": "npm run preview -- --port ${PORT:-3000} --host 0.0.0.0"
  }
}
